name: CodeQL

permissions:
  contents: read

on:
  workflow_dispatch:
  schedule:
    - cron: '42 13 * * 0'

jobs:
  job:
    uses: arvato-systems-jacs/reusable-workflows/.github/workflows/codeql.yml@master
    secrets: inherit
