package test.jaxbtemporal XML FIELD {

    class TestFieldXml XML FIELD xmlRoot {
        default public;

        Day             myDate;
        Timestamp(0)    myDateTime;
        Time(0)         myTime;
    }

    class TestPropXml XML PROPERTY xmlRoot {
        default public;

        Day             myDate;
        Timestamp(0)    myDateTime;
        Time(0)         myTime;
    }
}
