package reuse {
    default public;

    class OneElement {
        Ascii(8)    word;
    }


    class Body {
        (OneElement)   w1;
        (OneElement)   w2;
        (OneElement)   w3;
        (OneElement) List<> wl;
    }
}
